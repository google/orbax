# Orbax Model CLI

A command-line tool for inspecting Orbax models.

## Examples

To inspect the model:

  ```bash
  % obm_cli show /path/to/model
  ```

To show more details about an object in the model:

  ```bash
  % obm_cli show /path/to/model --details myobject
  ```
